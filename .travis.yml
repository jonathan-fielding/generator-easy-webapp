language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-easy-webapp' ]; then cd .. && eval "mv $currentfolder generator-easy-webapp" && cd generator-easy-webapp; fi

